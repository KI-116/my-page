| 关键词 | 工具/算法 | 场景 | 触发句 | 伪代码/关键步骤 | 示例题与要点 | 陷阱/优化 |
|---|---|---|---|---|---|---|
| 二分查找 | 二分查找 | 已排序的数据或函数单调；查找、定位满足条件的边界/阈值 | 有序数组，要求 O(log n) 查找 | `lo=0, hi=n-1; while lo<=hi: mid=(lo+hi)//2; if a[mid]==x return mid; if a[mid]>x: hi=mid-1; else lo=mid+1;` | LeetCode 704 二分查找；33. 搜索旋转排序数组 (先找旋转点再二分) | mid计算要防越界；注意循环结束条件(lo<=hi vs lo<hi)；区分求≥/≤边界的处理 |
| 左右双指针 | 双指针 (头尾) | 对撞或成对遍历，比如有序数组两数之和、三数之和等 | 数组/字符串子串，两端同时遍历 | `left=0, right=n-1; while left<right: sum=nums[left]+nums[right]; if sum==target: return [...]; if sum<target: left++ else right--;` | LeetCode 167 有序数组两数之和；15. 三数之和 (固定一数后左右指针求两数和) | 去重时跳过重复元素；注意顺序（数组需排序）；防止越界 |
| 快慢指针 | Floyd’s 算法 | 链表或序列存在环/循环检测 | 要判断是否存在环或找到环入口，一般用快慢指针 | `slow=fast=head; while fast and fast.next: slow=slow.next; fast=fast.next.next; if slow==fast: break;` | LeetCode 141. 环形链表 (判断有无环); 142. 环形链表II (找到环入口：相遇后重置并同步移动) | 注意空指针检测；设置正确的退出条件；找到相遇点后需重置快指针 |
| 滑动窗口 | 滑动窗口 (双指针) | 字符串/数组子串/子数组问题，需要连续子段的最值、计数等 | 连续子串/子数组，寻找满足条件的区间 | `l=0; for r in range(n): 更新窗口数据； while 窗口不满足条件: 更新数据并 l++； 更新答案` | LeetCode 3. 无重复字符的最长子串；209. 长度最小的子数组 (滑动窗口) | 维护窗口边界时需调整条件；使用哈希/数组记录频次；时间O(n) |
| 前缀和 | 前缀和/差分 | 频繁区间求和或区间更新，只读查询时加速 | 区间求和/更新问题，使用前缀和或差分 | `pre[0]=0; for i: pre[i+1]=pre[i]+a[i]; 区间和=pre[j]-pre[i]; // 差分: b[l]+=v; b[r+1]-=v` | LeetCode 303. 区域和检索-数组不可变；370. 区间加法 (差分数组) | 注意前缀长度+1；差分数组要处理 r+1 边界；空间O(n) |
| 哈希表 | 哈希映射/集合 | 需要快速查找/计数元素或检测重复等 | 需要快速查找或记录元素，或判断出现频次 | `map.clear(); for i,val in enumerate(a): if target-val in map: return [...] else map[val]=i;` | LeetCode 1. 两数之和；560. 和为 K 的子数组 (前缀和+哈希) | 注意哈希冲突；大表耗内存；遍历时检查 map |
| 排序 | 排序 | 当没有明显特性可用时，可排序再处理 | 对数组进行排序后解决问题 | `sort(nums); // 之后按题意线性处理` | LeetCode 56. 合并区间 (按起点排序)；128. 最长连续序列 (排序后) | 时间O(n log n)；选择稳定排序与否；排序后注意原序列关联 |
| 单调栈 | 栈 | 查找每个元素前后第一个更大/更小元素，或维护递增/递减序列 | 找到每个元素下一个更大/更小元素 | `for i: while stack nonempty and a[i] > a[stack.top()]: stack.pop(); push i;` | LeetCode 84. 柱状图中最大的矩形；739. 每日温度 | 注意空栈和边界；堆栈内保持单调递增/递减；矩形面积计算时用虚拟边界 |
| 单调队列 | 双端队列 | 滑动窗口问题，快速获取当前窗口最大或最小值 | 维护滑动窗口的最大值或最小值 | `deque.clear(); for i: while deque nonempty and a[i]>a[deque.back()]: pop_back(); deque.push_back(i); if i>=k-1: res=max(res,a[deque.front()]); if deque.front()==i-k+1: pop_front()` | LeetCode 239. 滑动窗口最大值；862. 和至少为 K 的子数组 | 队列存索引；保持单调；在滑出窗口时弹出 |
| 广度优先搜索 | BFS (队列) | 无权图或树的层次遍历、最短路径、连通性等 | 无权图或树的分层搜索，求最短路径 | `q.push(start); visited[start]=1; while q: u=q.pop(); for v in adj[u]: if not visited[v]: visited[v]=1; q.push(v);` | LeetCode 200. 岛屿数量 (网格连通分量)；127. 单词接龙 (最短路径) | 需标记已访问防止重复；可多源入队；空间可大 |
| 深度优先搜索 | DFS (递归/栈) | 图/树中遍历、路径搜索、连通分量、回溯 | 图或树遍历，探寻所有可能路径 | `dfs(u): visit[u]=1; for v in adj[u]: if not visited: dfs(v)` | LeetCode 207. 课程表 (有向图环检测)；剑指 Offer 12. 矩阵中的路径 | 注意递归深度；有向图需检测环（颜色标记）；回溯时撤销状态 |
| 树的递归遍历 | 树 DFS (递归) | 二叉树或 N 叉树遍历、树上 DP、分治 | 树结构问题的自然递归遍历 | `dfs(node): if not node: return; 处理(node); dfs(node.left); dfs(node.right)` | LeetCode 94. 二叉树中序遍历；226. 翻转二叉树；剑指 Offer 26. 树的子结构 | 检查空节点；可选择前/中/后序；注意返回值传递 |
| 回溯/递归搜索 | 回溯 | 枚举组合、排列、子集等所有可能 | 需要枚举所有组合、排列、子集等 | `dfs(path): if 满足则记录; for choice in choices: 选择; dfs(newPath); 取消选择` | LeetCode 46. 全排列；39. 组合总和；子集问题 | 提前剪枝：对不可能的分支返回；避免重复：排序后跳过相同 |
| 动态规划 | DP | 最优子结构，求最值或计数（路径数、方案数等） | 涉及最优子结构、状态转移，求最大/最小或方案数 | `dp[0]=...; for i: for j: dp[j]=f(dp[j], dp[j-w]+v)` | LeetCode 300. 最长递增子序列 (O(n log n) 解法)；416. 分割等和子集 | 状态定义清晰；可用滚动数组降维；注意初始化和转移 |
| 贪心 | 贪心算法 | 每步选择当前最优策略，构建全局最优解 | 问题可分阶段，每步选择当前最优 | `sort by key; for item in items: if 条件满足: take(item)` | LeetCode 435. 无重叠区间 (按结束时间排序)；55. 跳跃游戏 (记录最远可达) | 需验证局部最优可导全局最优；注意排序顺序；考虑极端情况 |
| 优先队列/堆 | 堆/优先队列 | 动态获取最小/最大元素或求TopK | 需要频繁获得当前最小或最大元素 | `minHeap or maxHeap; insert/pop O(log n)` | LeetCode 215. 数组中的第K大元素；295. 数据流中位数 | 插入/删除 O(log n); 空间O(n)；TopK 只维护大小为K的堆 |
| 链表操作 | 链表双指针/快慢指针 | 链表或原地遍历/操作问题 | 链表操作（如去重、环检测）常用双指针 | `dummy=head; while head: if head.val==head.next.val: head.next=head.next.next; else: head=head.next` | LeetCode 21. 合并两个有序链表；142. 环形链表II | 注意链表边界（头/尾）；空节点检查；环检测防无限循环 |
| 并查集 | 并查集 (DSU) | 维护动态连通性或集合归属问题 | 连通性/分组问题，用并查集维护集合合并 | `init parent[i]=i; find(x): if x!=parent[x]: parent[x]=find(parent[x]); union(a,b): parent[find(a)]=find(b)` | LeetCode 200. 岛屿数量；547. 省份数量 (联通分量) | 路径压缩+按秩合并；初始化时各节点独立；检查输入越界 |
| 拓扑排序 | 拓扑排序 (Kahn/DFS) | DAG 图中任务调度或排序 | DAG 依赖关系问题，用拓扑排序 | `compute indeg; queue=all indeg0 nodes; while queue: u=pop; for v in adj[u]: indeg[v]--; if indeg[v]==0: push v` | LeetCode 210. 课程表 II (找到上课顺序)；存在环则无解 | 检测环：若结果节点数<总节点数则有环；也可用 DFS 检测环； |
| 位运算 | 位操作 | 比特级处理，如基于二进制性质的巧妙操作 | 需位运算技巧，如快速判断 2 的幂或 XOR 技巧 | 常用操作：`x^y`, `x&y`, 左移 `<<`, `>>` 等；如检测 2 的幂：`x&(x-1)==0` | LeetCode 136. 只出现一次的数字 (利用 XOR)；190. 颠倒二进制位 | 负数右移注意符号位；结合算术位操作；内建函数 __builtin_popcount |
| 数学/数论 | 数学公式/数论 | 数值计算、质因数、组合数学等 | 涉及大数运算、质数、因子、组合等 | `g=gcd(a,b); lcm=a/g*b; modpow(a,b,mod)` | LeetCode 50. Pow(x, n) (快速幂); 202. 快乐数 (取余检测循环) | 注意整数溢出；模运算保持结果小；质数判断可至√n；组合数用DP/Lucas定理 |
| LRU缓存设计 | 哈希表 + 双向链表 | 设计带高效访问和淘汰策略的数据结构 | 需要实现 O(1) 访问及淘汰策略 | `哈希表存节点指针，双链表存使用顺序；访问/插入时移到表头；满则删除表尾` | LeetCode 146. LRU 缓存机制 (哈希+双链表) | 注意链表节点增删；容量满时删除尾节点；哈希冲突 |
| 模拟/暴力 | 直接模拟 | 数据规模小或没有明显模式时使用 | 规模小可直接模拟（暴力枚举） | `for i in ... for j in ... 检查条件或递归尝试` | 例如力扣 242. 有效的字母异位词 (计数) | 复杂度高易超时；只在 N 很小时可行；可用剪枝优化 |
| 栈 | 栈 (LIFO) | 需要后进先出访问，比如括号匹配、路径追溯等 | 后进先出场景，如括号匹配 | `push元素; pop元素; 注意检查栈空` | LeetCode 20. 有效的括号 (入栈匹配); 94. 二叉树中序遍历(迭代) | 注意栈空检查；模拟递归时需存状态 |
| 队列 | 队列 (FIFO) | 需要先进先出顺序管理，如BFS层序遍历 | 先进先出场景，如BFS层序 | `enqueue; dequeue; 注意循环队列满空判断` | LeetCode 119. 杨辉三角 (层序生成)；BFS 通用模板 | 注意队首队尾指针；环形缓冲区时分清满空 |
